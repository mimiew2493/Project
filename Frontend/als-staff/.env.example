# Base URL of the deployed backend (Next.js API)
# Local dev: leave unset, defaults to http://localhost:3000
# Production: the Vercel URL of the backend project, no trailing slash
VITE_API_URL="http://localhost:3000"
